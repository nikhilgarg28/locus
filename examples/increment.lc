fn increment(n: u8) -> (out: u8, @(out == n.wrapping_add(1))) {
    let out = n.wrapping_add(1);
    (out, _)
}

fn consume(n: u8) -> u8 {
    let (value, _) = increment(n);
    value
}

//~ proofs: 1
//~ run: increment(7) => (8, Erased)
//~ run: increment(255) => (0, Erased)
//~ run: consume(41) => 42
//~ rust: fn increment(n: u8) -> (u8, Erased) {
