fn preserve(n: u8) -> (out: u8, @(out == n)) {
    if n == 0 {
        (0, _)
    } else {
        (n, _)
    }
}

//~ proofs: 2
//~ run: preserve(0) => (0, Proved)
//~ run: preserve(200) => (200, Proved)
