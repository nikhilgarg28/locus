L0505: Cargo or project configuration failed

Locus could not resolve the selected Cargo package, parse its metadata, read a proof store, or run the requested project operation. Check the manifest path and Cargo diagnostic. Package metadata accepts only lib and targets; source paths must remain inside the package.

The filesystem regression is `tests/fixtures/project-errors/L0505`. Its `export.lc` reproduces this category through the actual project pipeline.

```rust
pub fn value()->u8 {1}
```

Language, as built: [1.28:9](../spec/17-modules.md).
