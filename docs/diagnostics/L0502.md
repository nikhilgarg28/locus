L0502: Cannot resolve module name

The module graph contains an unknown or duplicate binding, unresolved import cycle, or reserved compiler identifier. Check the explicit use path, alias, and crate/self/super root.

The filesystem regression is `tests/fixtures/project-errors/L0502`. Its `export.lc` reproduces this category through the actual project pipeline.

```rust
mod one {} mod one {}
```

Language, as built: [1.28:5](../spec/17-modules.md).
