// This specification is intentionally false. It is accepted only at an
// explicit trust boundary, recorded with this reason by `locus audit`.
trusted "deliberately false example: reviewer must reject this specification"
fn counted(xs: &Vec<u8>) -> (out: usize, @(out == 99)) = Vec::len;
fn inspect() -> (out: usize, @(out == 99)) {
    let xs: Vec<u8> = Vec::from([1, 2]);
    counted(&xs)
}
//~ run: inspect() => (2, Erased)
