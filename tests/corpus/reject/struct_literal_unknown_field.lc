// A struct literal with a field the struct does not have.
struct Lock {
    failures: u8,
    open: bool,
}

fn first() -> Lock {
    Lock { failures: 0, open: false, attempts: 0 } //~ error: L0224 `Lock` has no field `attempts`
}
