error[L0232]: `second` promises terminates and calls `first`, which does not
 --> tests/corpus/reject/fn_called_from_math_fn.lc:6:5
  |
6 |     first(n) //~ error: L0232 `second` promises terminates and calls `first`, which does not
  |     ^^^^^^^^
  |
  = note: a promise is never inferred: `first` keeps terminates only if it says so, with `#[terminates]` or `#![terminates]` at the top of its file
