// A hole takes no connective apart. The removed search proved `p && q`
// from `p` and `q`, `p || q` from either, and an implication by assuming
// its premise; each is a constructor or a `math fn` now, and the
// diagnostic names which.
math fn both(p: Prop, q: Prop, hp: @p, hq: @q) -> @(p && q) {
    _ //~ error: L0230 cannot show `p && q`
}

math fn either(p: Prop, q: Prop, hq: @q) -> @(p || q) {
    _ //~ error: L0230 cannot show `p || q`
}

math fn implied(p: Prop, q: Prop, hq: @q) -> @(p => q) {
    _ //~ error: L0230 cannot show `p => q`
}
