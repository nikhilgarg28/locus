error[L0236]: `+` on `u8` may panic, on overflow, so it is not a proposition
 --> tests/corpus/reject/operator_in_formula.lc:8:13
  |
8 |     prop!(a + b <= 255) //~ error: L0236 `+` on `u8` may panic, on overflow, so it is not a proposition
  |             ^
  |
  = note: write `a as Int + b as Int` for the exact sum, or `a.wrapping_add(b)` for the wrapped one
error[L0236]: `*` on `u8` may panic, on overflow, so it is not a proposition
  --> tests/corpus/reject/operator_in_formula.lc:12:14
   |
12 |     prove!(a * b <= 255); //~ error: L0236 `*` on `u8` may panic, on overflow, so it is not a proposition
   |              ^
   |
   = note: write `a as Int * b as Int` for the exact product, or `a.wrapping_mul(b)` for the wrapped one
error[L0236]: `/` on `i32` may panic, on a zero divisor, so it is not a proposition
  --> tests/corpus/reject/operator_in_formula.lc:16:38
   |
16 | fn in_a_claim(a: i32, b: i32, h: @(a / b == 2)) -> i32 { //~ error: L0236 `/` on `i32` may panic, on a zero divisor, so it is not a ...
   |                                      ^
   |
   = note: write `a as Int / b as Int` for the exact quotient on `Int`, which is total: `a / 0` is `0` and `a % 0` is `a`
error[L0236]: `-` on `i8` may panic, on overflow, so it is not a proposition
  --> tests/corpus/reject/operator_in_formula.lc:21:11
   |
21 |     prop!(-a == 3) //~ error: L0236 `-` on `i8` may panic, on overflow, so it is not a proposition
   |           ^
   |
   = note: write `-(a as Int)` for the exact negation, or `a.wrapping_neg()` for the wrapped one
error[L0220]: expected `Prop`, found `Int`
  --> tests/corpus/reject/operator_in_formula.lc:27:11
   |
27 |     prop!(a as Int + 1) //~ error: L0220 expected `Prop`, found `Int`
   |           ^^^^^^^^^^^^
