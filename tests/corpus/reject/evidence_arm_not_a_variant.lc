// An arm on evidence that is neither a variant nor `_`.
math fn first(p: Prop, q: Prop, both: @(p && q)) -> @p {
    match both {
        (hp, hq) => hp, //~ error: L0212 an arm here is `And::Variant(names...)` or `_`
    }
}
