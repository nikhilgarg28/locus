error[L0240]: use of moved value: `t`
  --> tests/corpus/reject/use_after_move.lc:13:26
   |
12 |     let first = consume(t);
   |                         - value moved here
13 |     let second = consume(t); //~ error: L0240 use of moved value: `t`
   |                          ^ value used here after move
   |
   = note: move occurs because `t` has type `Token`, which does not implement the `Copy` trait; a struct or enum is `Copy` with `#[derive(Clone, Copy)]`, and a `let mut` is whole again once assigned
error[L0240]: use of moved value: `t`
  --> tests/corpus/reject/use_after_move.lc:20:5
   |
19 |     let kept = t;
   |                - value moved here
20 |     t.id //~ error: L0240 use of moved value: `t`
   |     ^^^^ value used here after move
   |
   = note: move occurs because `t` has type `Token`, which does not implement the `Copy` trait; a struct or enum is `Copy` with `#[derive(Clone, Copy)]`, and a `let mut` is whole again once assigned
