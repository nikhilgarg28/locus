// The retired `#[condition]` proof type reads as an attribute, which stands
// before an item only.
fn first(n: u8) -> (out: u8, #[out == n]) { //~ error: L0121 an attribute goes before an item
    (n, _)
}
