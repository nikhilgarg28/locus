// The retired proposition literal `[condition]` and proof type
// `@[condition]`, each with a fix, and the rest of the file still read.
#[terminates] #[no_panic] #[no_io] fn same(x: u8, y: u8) -> Prop {
    [x == y] //~ error: L0117 a proposition is written `prop!(condition)`
}

fn first(n: u8) -> (out: u8, @[out == n]) { //~ error: L0117 a proof type is written `@(condition)`
    let claim: Prop = [ //~ error: L0117 a proposition is written `prop!(condition)`
        forall (k: u8) { k == k => [k <= 255] } //~ error: L0117 a proposition is written `prop!(condition)`
    ];
    (n, _)
}
